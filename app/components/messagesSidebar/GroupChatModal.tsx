'use client';

interface GroupChatModalProps {
  isOpen?: boolean;
  onClose: () => void;
}

export default function GroupChatModal({
  isOpen,
  onClose,
}: GroupChatModalProps) {
  return <div>GroupChatModal</div>;
}
